# ONC SSAMBA - Self-supervised anomaly detection for hydrophone spectrograms
# 
# Install options:
#   pip install -r requirements.txt                  # Standard install (GPU with CUDA)
#   pip install -e .                                 # Editable package install
#   pip install -e ".[mamba]"                        # With Mamba/CUDA dependencies
#
# NOTE: mamba_ssm and causal_conv1d must be built from source for your torch/CUDA version.
# Use --no-binary flag if pip tries to use incompatible prebuilt wheels.

# Core PyTorch
torch>=2.0.0
torchaudio>=2.0.0
numpy>=1.20.0

# Data processing
h5py>=3.8.0
matplotlib>=3.7.0
pandas>=2.0.0
scikit-learn>=1.0.0
tqdm>=4.65.0
scipy>=1.7.3

# ML/DL utilities
einops>=0.8.0
timm>=0.9.0

# Development tools
ipython>=8.0.0
python-dotenv>=1.0.0

# Monitoring
wandb>=0.15.0

# Tutorial/notebook extras (incl. labeling app + downloader) live in:
#   pip install -e ".[tutorial]"

# Mamba/CUDA dependencies (optional - requires NVIDIA GPU + CUDA toolkit + nvcc)
# These MUST be built from source to match your torch version:
#   pip install --no-cache-dir --no-binary causal_conv1d causal_conv1d
#   pip install --no-cache-dir --no-binary mamba_ssm mamba_ssm==2.2.5
causal_conv1d>=1.5.0
mamba_ssm==2.2.5
